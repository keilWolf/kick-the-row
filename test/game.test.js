import Game from './game'
/*

describe("Game", () => {
  it("should be instanceable", () => {
    expect(new Game()).toBeInstanceOf(Game);
  });
});
*/
describe("Game", () => {
  it("should be instanceable", () => {
    expect(1+2).toBe(3);
  });
});